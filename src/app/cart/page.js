import CartClient from '@/components/CartClient';

export const metadata = {
  title: 'Cart — VULCRO',
  description: 'Your cart — VULCRO premium kurti sets. Review items and proceed to checkout.',
};

export default function CartPage() {
  return <CartClient />;
}
