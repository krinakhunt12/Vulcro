import WishlistClient from '@/components/account/WishlistClient';

export const metadata = {
  title: 'My Wishlist — VULCRO',
  description: 'Your saved items at VULCRO',
};

export default function Page() {
  return <WishlistClient />;
}
